import { StyleSheet } from "react-native";

const FindyLogoStyle = StyleSheet.create({
    container: {
        backgroundColor: "#FFDA66",
        borderColor: "#C8A536",
        borderRadius: 10,
        marginBottom: 20,
        shadowColor: "#000",
        shadowOffset: {
            width: 0,
            height: 2,
        },
        shadowOpacity: 0.25,
        shadowRadius: 3.84,
        elevation: 5,
    },
    text: { fontSize: 20, fontWeight: "bold" },
});

export default FindyLogoStyle;
