import { View } from 'react-native';
import { Text } from '@rneui/base';

const Map = ({navigation}) => {
    return (
      <View>
        <Text>Map</Text>
      </View>
    );
  };

export default Map